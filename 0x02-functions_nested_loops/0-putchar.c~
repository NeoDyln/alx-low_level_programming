#include <stdio.h>
#include <string.h>
/**
 * main - Entry Point
 * 
 * Return: Always 0 (Success)
 **/
int main()
{
char st[9] = "_putchar";
for(int i = 0; i < 8; i++)
{
putchar(st[i]);
}
putchar('\n');
return (0);
}
